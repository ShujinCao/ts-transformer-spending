data:
  path: "data/raw/health_expenditure_sample.csv"  # or ...all_countries.csv
  input_len: 10
  forecast_horizon: 5

model:
  d_model: 64
  n_heads: 4
  num_layers: 3
  dropout: 0.1
  forecast_horizon: 5

training:
  batch_size: 32
  epochs: 40
  learning_rate: 0.001

